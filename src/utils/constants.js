export const MAX_MESSAGES = 50; // Maximum number of messages to store
export const MAX_TARGETS = 20; // Maximum number of targets to track
export const MESSAGE_THROTTLE = 100; // Minimum time between messages in ms

export const RADAR_CONFIG = {
  width: 300,
  height: 400,
  scale: 40,
  gridSize: 8,
  targetWidth: 15,
  targetHeight: 25,
}; 